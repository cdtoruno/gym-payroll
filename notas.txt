  --- POR HACER ---- 

2 dias al mes
que cuando se registra unas vacaciones, estas se deben linkear automatico a la nomina para hacer la deduccion de los dias faltantes
Ahora si falta un dia de vacaciones pagadas, se resta la mitad de las vacaciones

Si falta un día de la primera quincena, se le descuenta en la siguiente quincena del siguiente mes

Ejemplo: Si falta el 11 de marzo, se le descuenta en Abril y ya solo tendría un día de vacaciones


PARA CUANDO ME PIDA AGUINALDO



https://delepesoasuspesos.com/calcular-aguinaldo-nicaragua


# Para borrar todos los datos para hacer test

from apps.vacations.models import Vacation, VacationAbsence
from apps.payroll.models import PayrollRecord
from apps.attendance.models import LateArrival

# Borrar todo menos empleados
LateArrival.objects.all().delete()
VacationAbsence.objects.all().delete()
Vacation.objects.all().delete()
PayrollRecord.objects.all().delete()

print("Datos borrados:")
print(f"   LateArrival:     0")
print(f"   VacationAbsence: 0")
print(f"   Vacation:        0")
print(f"   PayrollRecord:   0")

exit()